{
  "name": "beyond-the-boring",
  "private": true,
  "workspaces": {
    "packages": [
      "projects/*"
    ],
    "nohoist": [
      "**"
    ]
  },
  "scripts": {
    "build": "lerna run build --parallel",
    "dev": "lerna run dev --parallel",
    "prepare": "husky install && yarn hack:tailwind-intellisense",
    "test": "jest",
    "hack:tailwind-intellisense": "ln -sf projects/garden/tailwind.config.js tailwind.config.js"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lerna": "^4.0.0"
  }
}
