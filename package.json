{
  "name": "beyond-the-boring",
  "private": true,
  "workspaces": [
    "projects/**"
  ],
  "scripts": {
    "build": "lerna run build --parallel",
    "dev": "lerna run dev --parallel",
    "prepare": "husky install && yarn hack:tailwind-intellisense",
    "hack:tailwind-intellisense": "ln -s projects/garden/tailwind.config.js tailwind.config.js"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lerna": "^4.0.0",
    "prettier": "^2.5.1",
    "pretty-quick": "^3.1.3"
  }
}
